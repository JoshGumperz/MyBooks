<h1>from search.handlebars</h1>
{{#each items as |item|}}
<p>{{item.volumeInfo.title}}</p>
<a href="#" id="{{item.id}}" class="book-photo">
<img src='{{item.volumeInfo.imageLinks.thumbnail}}'>
</a>
{{/each}}

